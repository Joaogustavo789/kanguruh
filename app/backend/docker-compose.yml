version: '3'
services:
  db:
    image: mysql:8.0.29
    container_name: kanguruh_db
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    ports:
      - 3306:3306
    environment:
       - MYSQL_ROOT_PASSWORD=${DB_PASSWORD}
